import { ScrollView, Text, View, Image } from "react-native";

export default function Commande() {
  return (
    <ScrollView>
      <View>
        <Text>Commande</Text>
      </View>
    </ScrollView>
  );
}